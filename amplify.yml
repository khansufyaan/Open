version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - env | grep -e PRIVY_APP_SECRET >> .env.production || true
        - env | grep -e TURNKEY_API_PRIVATE_KEY >> .env.production || true
        - env | grep -e TURNKEY_API_PUBLIC_KEY >> .env.production || true
        - env | grep -e ADMIN_SECRET >> .env.production || true
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
